# Local dev: leave DATABASE_URL empty → uses SQLite (certificates.db)
# Production (Render + Neon): set DATABASE_URL in Render Dashboard

# Copy from Neon → Dashboard → Connection string (URI)
# DATABASE_URL=postgresql://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

FLASK_DEBUG=1
